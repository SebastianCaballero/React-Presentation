import './Components.css'

const Expectations = (props) => {
    return (
        <div className="card light-blue">
            <h3>{props.title}</h3>
            <p>Become a great Full-Stack Developer</p>
        </div>
    )
};

export default Expectations